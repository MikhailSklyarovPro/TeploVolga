# 🎮 Команды проекта TeploVolga

## 🚀 Разработка

| Команда           | Описание                       |
| ----------------- | ------------------------------ |
| `npm run dev`     | Запуск сервера разработки      |
| `npm run build`   | Сборка проекта для продакшена  |
| `npm run preview` | Предпросмотр production сборки |

## 🔍 Проверка кода

### JavaScript (ESLint)

| Команда            | Описание                              |
| ------------------ | ------------------------------------- |
| `npm run lint`     | Проверить JS код на ошибки            |
| `npm run lint:fix` | Автоматически исправить ошибки ESLint |

### CSS (Stylelint)

| Команда                 | Описание                                 |
| ----------------------- | ---------------------------------------- |
| `npm run stylelint`     | Проверить CSS код на ошибки              |
| `npm run stylelint:fix` | Автоматически исправить ошибки Stylelint |

### Форматирование (Prettier)

| Команда                | Описание                               |
| ---------------------- | -------------------------------------- |
| `npm run format`       | Форматировать все файлы                |
| `npm run format:check` | Проверить форматирование без изменений |

## 🔧 Git Hooks

| Хук          | Когда запускается     | Что делает                                                  |
| ------------ | --------------------- | ----------------------------------------------------------- |
| `pre-commit` | Перед каждым коммитом | Запускает ESLint, Stylelint и Prettier на измененных файлах |

## ⚡ Быстрые команды

### Перед коммитом (рекомендуется)

```bash
npm run lint:fix && npm run stylelint:fix && npm run format
```

### Полная проверка проекта

```bash
npm run lint
npm run stylelint
npm run format:check
```

### Исправить всё одной командой

```bash
npm run lint:fix
npm run stylelint:fix
npm run format
```

## 📝 Примеры использования

### Начало работы

```bash
# Установка зависимостей
npm install

# Запуск dev сервера
npm run dev
```

### Во время разработки

```bash
# Автоматическое исправление по ходу работы
npm run lint:fix
npm run format
```

### Перед коммитом

```bash
# Git автоматически запустит проверки
git add .
git commit -m "feat: добавил новую функцию"

# Если хотите проверить вручную перед коммитом
npm run lint
npm run stylelint
npm run format:check
```

### Сборка для продакшена

```bash
# Исправить все ошибки
npm run lint:fix
npm run stylelint:fix
npm run format

# Собрать проект
npm run build

# Проверить результат
npm run preview
```

## 🎯 Рекомендуемый workflow

1. **Пишете код** → Редактор автоматически форматирует при сохранении
2. **Периодически** → `npm run lint:fix` для проверки
3. **Перед коммитом** → Git hooks автоматически проверят код
4. **Коммит успешен** ✅

## ⚠️ Что делать при ошибках

### ESLint блокирует коммит

```bash
# Посмотреть ошибки
npm run lint

# Исправить автоматически
npm run lint:fix

# Если остались ручные ошибки - исправить в коде
```

### Stylelint блокирует коммит

```bash
# Посмотреть ошибки
npm run stylelint

# Исправить автоматически
npm run stylelint:fix

# Если остались ручные ошибки - исправить в коде
```

### Prettier блокирует коммит

```bash
# Форматировать все файлы
npm run format
```

### Пропустить pre-commit (не рекомендуется)

```bash
git commit --no-verify -m "сообщение"
```

## 💡 Полезные советы

1. **Настройте автосохранение в редакторе** - код будет форматироваться автоматически
2. **Установите рекомендуемые расширения VS Code** - см. `.vscode/extensions.json`
3. **Запускайте `npm run lint:fix` почаще** - меньше проблем при коммите
4. **Читайте сообщения об ошибках** - они подсказывают, что именно не так

## 🔗 Связанные файлы

- `package.json` - все доступные скрипты
- `eslint.config.js` - конфигурация ESLint
- `.prettierrc` - конфигурация Prettier
- `.stylelintrc.json` - конфигурация Stylelint
- `.husky/pre-commit` - pre-commit хук
