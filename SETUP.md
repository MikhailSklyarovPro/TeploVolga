# Настройка проекта TeploVolga Front

## ✅ Что установлено и настроено

### 1. Линтеры и форматтеры

#### ESLint (v9.37.0)

- **Файл конфигурации**: `eslint.config.js`
- **Формат**: Flat Config (новый формат ESLint 9)
- **Основные правила**:
  - Использование `const`/`let` вместо `var`
  - Строгое сравнение (`===`)
  - Запрет `eval` и других небезопасных конструкций
  - Предпочтение стрелочных функций
  - Использование template literals
- **Запуск**: `npm run lint` или `npm run lint:fix`

#### Prettier (v3.6.2)

- **Файл конфигурации**: `.prettierrc`
- **Игнорируемые файлы**: `.prettierignore`
- **Настройки**:
  - Одинарные кавычки для JS
  - 2 пробела для отступов
  - Точка с запятой в конце
  - 80 символов на строку
  - LF окончания строк
- **Запуск**: `npm run format` или `npm run format:check`

#### Stylelint (v16.25.0)

- **Файл конфигурации**: `.stylelintrc.json`
- **Базовая конфигурация**: `stylelint-config-standard`
- **Основные правила**:
  - Запрет именованных цветов (использовать #hex или rgb)
  - Запрет ID селекторов
  - Обязательные кавычки для URL и font-family
  - Числовая нотация для font-weight
  - Контекстная нотация для media queries
- **Запуск**: `npm run stylelint` или `npm run stylelint:fix`

### 2. Git Hooks

#### Husky (v9.1.7)

- **Папка**: `.husky/`
- **Хуки**:
  - `pre-commit` - запускается перед каждым коммитом
- **Что делает**: Автоматически запускает lint-staged

#### lint-staged (v16.2.4)

- **Конфигурация**: в `package.json`
- **Что проверяет**:
  - `*.{js,jsx,ts,tsx}` → ESLint + Prettier
  - `*.{css,scss,sass}` → Stylelint + Prettier
  - `*.{html,json,md}` → Prettier
- **Работает**: Только с измененными файлами (staged)

### 3. VS Code интеграция

#### Файлы

- `.vscode/settings.json` - настройки редактора
- `.vscode/extensions.json` - рекомендуемые расширения

#### Рекомендуемые расширения

1. **ESLint** (dbaeumer.vscode-eslint)
2. **Prettier** (esbenp.prettier-vscode)
3. **Stylelint** (stylelint.vscode-stylelint)

#### Функции

- Автоматическое форматирование при сохранении
- Автоматическое исправление ошибок ESLint/Stylelint
- Отключение встроенной валидации CSS (используется Stylelint)

## 📋 Workflow

### Перед началом работы

```bash
npm install
```

### Во время разработки

```bash
npm run dev  # Запуск dev сервера
```

### Проверка кода вручную

```bash
npm run lint         # Проверка JS
npm run stylelint    # Проверка CSS
npm run format:check # Проверка форматирования
```

### Автоматическое исправление

```bash
npm run lint:fix      # Исправить JS
npm run stylelint:fix # Исправить CSS
npm run format        # Форматировать все файлы
```

### Коммит изменений

```bash
git add .
git commit -m "Описание изменений"
# Автоматически запустятся:
# 1. ESLint для JS файлов
# 2. Stylelint для CSS файлов
# 3. Prettier для всех файлов
```

## 🔧 Настройка редактора

### VS Code

1. Установите рекомендуемые расширения (откроется подсказка при открытии проекта)
2. Перезапустите VS Code
3. Теперь при сохранении файлы будут автоматически форматироваться

### WebStorm/IntelliJ IDEA

1. Настройки → Languages & Frameworks → JavaScript → Code Quality Tools → ESLint
   - Включить ESLint
   - Automatic ESLint configuration
2. Настройки → Languages & Frameworks → JavaScript → Prettier
   - Включить Prettier
   - On save
3. Настройки → Languages & Frameworks → Style Sheets → Stylelint
   - Включить Stylelint

## 🚨 Частые проблемы

### Pre-commit хук не работает

```bash
npx husky install
chmod +x .husky/pre-commit  # для Linux/Mac
```

### ESLint ругается на глобальные переменные

Добавьте переменную в `eslint.config.js` в секцию `globals`:

```js
globals: {
  myGlobalVar: 'readonly';
}
```

### Stylelint не работает

Убедитесь, что установлены все зависимости:

```bash
npm install
```

### Prettier и ESLint конфликтуют

Конфликтов быть не должно, так как Prettier запускается после ESLint.
Если возникла проблема, запустите:

```bash
npm run lint:fix
npm run format
```

## 📦 Установленные пакеты

```json
{
  "devDependencies": {
    "@eslint/js": "^9.37.0",
    "eslint": "^9.37.0",
    "husky": "^9.1.7",
    "lint-staged": "^16.2.4",
    "prettier": "^3.6.2",
    "stylelint": "^16.25.0",
    "stylelint-config-standard": "^39.0.1",
    "vite": "^7.1.7"
  }
}
```

## 🎯 Следующие шаги

1. ✅ Все инструменты установлены и настроены
2. ✅ Pre-commit хуки работают
3. ✅ VS Code интеграция настроена
4. 📝 Начинайте разработку!

## 💡 Советы

1. Используйте автосохранение в редакторе для автоматического форматирования
2. Запускайте `npm run lint:fix` перед коммитом для быстрого исправления ошибок
3. Если хук pre-commit блокирует коммит, исправьте ошибки, которые он показывает
4. Все конфигурации можно изменить под ваши нужды в соответствующих файлах
