# TeploVolga Front

Проект на Vanilla JS с Vite и полной настройкой линтеров.

## 🚀 Установленные инструменты

- **ESLint** - анализатор JavaScript кода
- **Prettier** - форматирование кода (JS, CSS, HTML, JSON, MD)
- **Stylelint** - анализатор CSS кода
- **Husky** - Git hooks для автоматической проверки перед коммитом
- **lint-staged** - запуск линтеров только на измененных файлах

## 📦 Установка

```bash
npm install
```

## 🛠️ Доступные команды

### Разработка

```bash
npm run dev          # Запуск dev сервера
npm run build        # Сборка для продакшена
npm run preview      # Предварительный просмотр production сборки
```

### Проверка кода

```bash
npm run lint         # Проверка JS кода с помощью ESLint
npm run lint:fix     # Автоматическое исправление ошибок ESLint

npm run format       # Форматирование всех файлов с помощью Prettier
npm run format:check # Проверка форматирования без изменений

npm run stylelint    # Проверка CSS кода с помощью Stylelint
npm run stylelint:fix # Автоматическое исправление ошибок Stylelint
```

## 🔧 Конфигурация

### ESLint

Конфигурация находится в `eslint.config.js`. Используется современный flat config формат ESLint 9.

Основные правила:

- `prefer-const` и `no-var` - использование современного ES6+
- `eqeqeq` - строгое сравнение
- `no-eval`, `no-implied-eval` - запрет небезопасного кода
- `prefer-template` - использование template strings
- Arrow functions правила

### Prettier

Конфигурация находится в `.prettierrc`:

- Одинарные кавычки для JS
- Точка с запятой в конце строк
- 2 пробела для отступов
- Максимальная длина строки: 80 символов

### Stylelint

Конфигурация находится в `.stylelintrc.json`:

- Расширяет `stylelint-config-standard`
- Запрет именованных цветов (использовать hex/rgb)
- Запрет ID селекторов
- Обязательные кавычки для URL и font-family
- Современная нотация media queries

## 🪝 Pre-commit хуки

При каждом коммите автоматически запускаются:

1. **ESLint** - проверка и автоматическое исправление JS файлов
2. **Stylelint** - проверка и автоматическое исправление CSS файлов
3. **Prettier** - форматирование всех измененных файлов

Если проверки не пройдены, коммит будет отклонен.

## 📁 Структура проекта

```
TeploVolgaFront/
├── src/
│   ├── main.js      # Основной JS файл
│   └── style.css    # Основные стили
├── index.html       # HTML шаблон
├── package.json     # Зависимости и скрипты
├── eslint.config.js # Конфигурация ESLint
├── .prettierrc      # Конфигурация Prettier
├── .stylelintrc.json # Конфигурация Stylelint
└── .husky/          # Git hooks
    └── pre-commit   # Pre-commit хук
```

## 🎯 Рекомендации

1. Используйте `npm run lint:fix` перед коммитом для автоматического исправления ошибок
2. Настройте ваш редактор для автоматического форматирования при сохранении
3. Все новые компоненты создавайте в отдельных файлах в папке `src/`
4. Следуйте правилам линтеров для поддержания чистоты кода

## 📝 Лицензия

Приватный проект
